fastapi==0.115.0
slowapi==0.1.9
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg[binary]==3.2.1
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
email-validator==2.2.0
alembic==1.13.2
bcrypt==4.0.1
minio==7.2.7
requests==2.32.3
# requests valida chardet >= 3.0.2, < 6.0.0. Sem esse teto a imagem instala
# chardet 7.x e todo comando loga RequestsDependencyWarning. É só ruído hoje,
# mas o mTLS do Emissor Nacional passa por essa pilha e o aviso atrapalharia
# o diagnóstico de um erro de TLS real.
chardet<6
zeep==4.3.1
openpyxl==3.1.5
reportlab==4.2.2
signxml==3.2.2
# A pilha de assinatura XMLDSig do signxml 3.2.2 exige versões antigas de DUAS
# libs, comprovado em venv local (build+sign+validate da DPS):
#   - cryptography: referencia curvas EC (ex.: SECT163K1) removidas na 43+, então
#     precisa de <43. Sem teto o container pega a 44 e `import signxml` quebra.
#   - pyOpenSSL: usa OpenSSL.crypto.verify/sign, removidos na 24, então <24. E a
#     pyOpenSSL 23.x por sua vez fixa cryptography em <42.
# Resultado da resolução: cryptography 41.0.7 + pyOpenSSL 23.3.0. Ao subir o
# signxml (>=4, que fala com libs modernas) estes dois tetos saem juntos.
cryptography>=41,<42
pyOpenSSL>=23,<24
lxml==5.3.0
